# Postman Files

Import these files into Postman:

1. `authservice-apis.postman_collection.json`
2. `authservice-local.postman_environment.json`

Then select `authservice-local` environment and run requests.

Default base URL is `http://localhost:8081`.
