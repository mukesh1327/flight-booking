apiVersion: v1
kind: Service
metadata:
  name: flight-service
  labels:
    app: flight-service
spec:
  selector:
    app: flight-service
  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: http
