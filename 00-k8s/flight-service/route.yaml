apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flight-service
  labels:
    app: flight-service
spec:
  host: flight.skyfly.example
  to:
    kind: Service
    name: flight-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
