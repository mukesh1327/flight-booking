apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  host: payment.skyfly.example
  to:
    kind: Service
    name: payment-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
